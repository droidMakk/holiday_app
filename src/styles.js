import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  progress_view: {
    flex: 1,
    alignContent: 'center',
    height: '100%',
    marginVertical: '50%',
  },
});

export default styles;
